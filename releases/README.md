# Releases

GitHub Actions isn't currently able to run for this repository, so compiled release APKs are
committed here for download.

| File | Notes |
| --- | --- |
| `grok-android-v0.1.0.apk` | Grok Build OAuth + `/v1/chat/completions` chats. Package `fail.failure.grok`. Debug-key signed release build. |

Sign in with device-code OAuth (`auth.x.ai`) or paste an `xai-...` key, then start a chat.
